[package]
name = "slate-client"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { workspace=true, features=["router"] }
slate-shared = { path = "../shared" }
slate-api = { path = "../api" }
nucleo-matcher = "0.3.1"

[features]
server = ["dioxus/server", "slate-api/server"]
web = ["dioxus/web"]
default = []
