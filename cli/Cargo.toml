[package]
name = "slate-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
slate-bridge = { path = "../bridge" }
tokio = { version = "1.52.3", features = ["macros", "rt-multi-thread"] }
