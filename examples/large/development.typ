#import "vault.typ": *;
#show: setup
#tag("wad")

== Package Management
#flashcard("What is pythons package registary", "PyPi")

#flashcard("What is the shit way of installing packages", "pip")
#flashcard("What does pip install do?", "Installs a package")
#flashcard("What does pip list do?", "Shows installed packages")
#flashcard("What does pip freeze do?", "Save package list to file")

== Virtual Envs
#flashcard("What is a virtual env", "Way to isolate python and package versions from different projects")

#flashcard("What is the real way to manage things", "UV or Nix!")

== Git
Git Good!

#flashcard("What is git", "Version Control")
#flashcard("What is git status", "Find out the local state")
#flashcard("What is git push", "Upload to remote")
#flashcard("What is git pull", "Get latest changes from remote")
#flashcard("What is git add", "Add files to a commit")
#flashcard("What is git commit", "Add changes to local")
#flashcard("What is git clone", "Copy remove repository")
