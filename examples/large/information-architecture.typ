#import "vault.typ": *;
#show: setup
#tag("wad")

= Information Architecture
#flashcard("What is a persona", "A made up user to allow developers to plan out application better")
#flashcard("What is a user needs matrix", "A document capturing the needs of users to prioritise")

#flashcard("What is a wireframe?", "A quickly made plan of the visuals and functions of a page")

#flashcard("What is a site map?", "A graph displaying how users can navigate the application")
#flashcard("What is useabilty testing?", "Get repersentative set of users in a room to evaluate a decision")
